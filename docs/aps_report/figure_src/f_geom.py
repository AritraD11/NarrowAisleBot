# Figure 1 is a real CAD render, not generated geometry — cad/renders/
# chassis_top_dimensioned.png, exported from SolidWorks and dimensioned from
# the exact coordinates in cad/extracted_geometry.md (read from the DXF, not
# estimated). This file used to draw a matplotlib reconstruction of the same
# plan view; that reconstruction was replaced on request (see cad/README.md's
# note on the removed OpenSCAD model — the same instruction applies here) and
# this is now a no-op so a full regeneration run does not overwrite the real
# image with a synthetic one.
print('fig01: real CAD render, not generated — see cad/renders/chassis_top_dimensioned.png')
